function mypack
% function mypack
% 
% Does as Matlab pack function recommends - pack'ing (dumping environment
% and reloading). Data is stored in the tmp directory. In contrast to
% Matlab pack this code could be called from scripts.
% 
% See also PACK
%
% Copyright 2007 Oliver Amft

error('Not completely implemented.');
% OAM REVISIT: fetch workspace or provide it as parameter.
save(tempname);
keep(tempname);
load(tempname);