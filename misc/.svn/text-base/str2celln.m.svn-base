function celllist = str2celln(varargin)
% function celllist = str2celln(varargin)
% 
% Convert input, e.g. strings into a cell array
% 
% See also str2cell, str2cellf, num2strcell
celllist = varargin';

% celllist = cell(length(varargin),1);
% for i = 1:length(varargin)
% 	celllist{i} = varargin{i};
% end;