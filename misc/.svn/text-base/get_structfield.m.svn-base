function matrix = get_structfield(Struct, SelStr)
% function matrix = get_structfield(Struct, SelStr)
%
% Returns data from Struct according to selector string SelStr (field name)
%
% Example: Select field huhu from the Struct
%       A = get_structfield(Struct, 'huhu');


matrix = eval(['Struct.' SelStr]);
