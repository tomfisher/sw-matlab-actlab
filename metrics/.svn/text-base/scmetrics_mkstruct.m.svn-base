function scmetric = scmetrics_mkstruct(scores, labels)
% function scmetric = scmetrics_mkstruct(scores, labels)
%
% Create an scmetrics struct (for a scoring classifier)

scmetric.scores = col(scores);
scmetric.labels = col(labels);

scmetric = scmetrics_update(scmetric);

