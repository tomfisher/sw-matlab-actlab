function veWeight = fselTemplate( maDataTrain, veLabelTrain, veWeight, varargin )
% function veWeight = fselTemplate( maDataTrain, veLabelTrain, veWeight, varargin )
% 
% Template for weighting/sorting routines 
% 
% All routines should consume the above paramters and return a feature
% weighing vector. Additional paramters should be handled by
% process_options().
% 
% Optional paramters:
% param1 - Parameter param1 does ...
% verbose - Set verbosity level of this routine (e.g. for debugging)

% (c) 2007/10/08, Oliver Amft, ETH Zurich

[Param1 verbose] = process_options(varargin, 'param1', 50, 'verbose', 1);

[NrObs, NrFeatures] = size(maDataTrain);

