function itemlist = segment_item(segment)
% function itemlist = segment_item(segment)
%
% Generate a sequence of values between segment boundaries

% OAM REVISIT: Did not find a name which fits function purpose better 
itemlist = segment(1):segment(2);